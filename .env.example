# AniLedger Environment Variables
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# AniList OAuth Client ID
# Get yours from: https://anilist.co/settings/developer
# Note: AniList provides both Client ID and Client Secret
#       Only use the Client ID - do NOT add the Client Secret
#       Native apps should not use client secrets (they cannot be kept secure)
ANILIST_CLIENT_ID=YOUR_CLIENT_ID_HERE
